describe('Index Router', function(){
  'use strict';
  var indexRouter = require('../../routes/index');

  it('should have an index route', function(){
    debugger;
    expect(indexRouter.index).not.toBe(undefined);
  });
});